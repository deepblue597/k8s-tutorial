apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database
spec:
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteMany
  storageClassName: "" # Empty storage class for manual binding
  selector:
    matchLabels:
      type: my-volume
